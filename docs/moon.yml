tasks:
  compile:
    command: 'typst compile main.typ bericht.pdf'
    inputs:
      - 'main.typ'
      - 'artifakte/**/*'
    outputs:
      - 'bericht.pdf'

  check:
    command: 'typst compile main.typ /dev/null -f pdf'
    inputs:
      - 'main.typ'
      - 'artifakte/**/*'

  spellcheck:
    command: 'typos .'
    inputs:
      - '**/*.typ'
      - '**/*.md'
